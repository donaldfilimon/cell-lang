# CLAUDE.md

`AGENTS.md` is canonical for this repository. Read it before editing anything
here: it carries the toolchain pin, the `-Dswift=false` requirement, the gate
commands, and the reasons a green `zig build test` is weak evidence in this tree.

Nothing Claude-specific applies beyond it.
