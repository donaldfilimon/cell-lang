// EXPECT: currently-rejected
// Violates: docs/OWNERSHIP.md R14 (assignment requires a mutable place).
//
// Reports `cannot assign to immutable binding 'x'` at the assignment, with a
// note at the `let`.

pub fn main() {
    let copy x = 1
    x = 2
}
