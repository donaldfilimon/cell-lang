// PARSES since 2026-09-16 and FAILS `cell check`: the body `return path`
// yields a `String` where `Result<String, Int>` is declared, and the
// checker reports a return type mismatch. `Ok`/`Err` exist
// (`examples/results.cell` runs); this file stays here because that return
// is still a type error, not because construction syntax is missing
// (SPEC section 3.4). It used to be a parse error at the `<`.
pub fn read_file(shared path: String) -> Result<String, Int> {
    return path
}
